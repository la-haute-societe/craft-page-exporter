{% macro configWarning(setting) -%}
    {% set setting = '<code>'~setting~'</code>' %}
    {{ "This is being overridden by the {setting} config setting in your {file} config file."|t('craft-page-exporter', {
        setting: setting,
        file: 'craft-page-exporter.php'
    })|raw }}
{%- endmacro %}
